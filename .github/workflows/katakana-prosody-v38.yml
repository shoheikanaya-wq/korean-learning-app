name: Legacy katakana prosody check
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: echo "Legacy v3.8 mutation workflow is retired. Current pronunciation UI is validated by validate-app.yml."
